<h1>Listing types</h1>

<table class='table'>
  <tr>
    <th>Name</th>
    
    <th></th>
  </tr>

<% @types.each do |type| %>
  <tr>
    <td><%= type.name %></td>
    <td><%= link_to 'Edit', edit_type_path(type), class: "btn btn-small"  %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Type', new_type_path %>
